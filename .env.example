# For npm run dev on the host (not docker compose).
# Docker compose uses compose/app.env instead — no .env file needed for compose.
PORT=3100
ADMIN_PASSWORD=local-dev-password
BACKEND_URL=http://localhost:3101
BACKEND_PASSWORD=local-dev-password
